---
import { getDocs } from "../../lib/docs";
import { url } from "../../lib/url";
const base = import.meta.env.BASE_URL;
const docs = getDocs();
---

<html>
  <head>
    <title>NexaSnap Docs</title>
  </head>

  <body>
    <h1>NexaSnap API Documentation</h1>

    <ul>
      {
        docs.map((doc) => (
          <li>
            <a href={url(`/docs/${doc.category}/${doc.slug}`)}>{doc.title}</a>
          </li>
        ))
      }
    </ul>
  </body>
</html>
